{
  "name": "mission-control",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A self-hosted LAN dashboard: live service status + uptime history, start/stop/restart and logs for Docker/host services, a shared file browser, chat, a customisable board of widgets, scheduled tasks, and a local-Ollama coding agent that reads and edits files in a sandboxed workspace. Zero build step — plain ES modules, three dependencies.",
  "license": "MIT",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "ws": "^8.21.3"
  }
}
